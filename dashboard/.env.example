# ─────────────────────────────────────────────────────────────────────────────
# Instantly Live Dashboard — environment configuration
#
# The app runs out-of-the-box with MOCK data when nothing here is set.
# Fill these in to switch to real Instantly + Supabase data.
# ─────────────────────────────────────────────────────────────────────────────

# Supabase (server-side). Used to store metric snapshots (history/trends) and
# client feedback. Without it, the app keeps working but feedback/history are
# kept in-memory only (lost on restart).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Fallback single-tenant Instantly API key (V2).
# Used for the "demo" client and as a default when a client has no key in DB.
# Get it from Instantly → Settings → Integrations → API Keys (V2).
INSTANTLY_API_KEY=

# Optional: protect the /api/sync cron endpoint with a shared secret.
# Vercel Cron will send `Authorization: Bearer <CRON_SECRET>`.
CRON_SECRET=

# ── Alerting (P0.1 · notify.ts) ──────────────────────────────────────────────
# The 07:00 automations cron pushes vital alerts (account 550s / negative status,
# open-rate collapse, quota, automation failures) to ONE channel. Unset = no-op
# (logs and skips) — safe to deploy before secrets are set. The recipient is
# re-pointable via env WITHOUT a code change.
#
# Channel is auto-detected from whichever creds are present, or force it:
#   ALERT_CHANNEL = telegram | email | webhook | console
ALERT_CHANNEL=
# Telegram (default): bot token from @BotFather + recipient chat id.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# webhook: a Slack/Discord/webhook.site incoming webhook (POST JSON).
ALERT_WEBHOOK_URL=
# email (Resend): API key + verified sender; recipient goes in ALERT_RECIPIENT.
ALERT_RESEND_API_KEY=
ALERT_EMAIL_FROM=
# Generic recipient: email address (email channel) or Telegram chat id override.
ALERT_RECIPIENT=

# ── Campaign Manager Agent (tab "Agent") ─────────────────────────────────────
# Claude key used to generate/review reply drafts. WITHOUT it the agent degrades
# to template drafts (never crashes). Get it from console.anthropic.com.
ANTHROPIC_API_KEY=
# Optional: override the model (default: claude-sonnet-5).
AGENT_MODEL=
# Optional: max reply drafts per GET /agent load (default: 8; use ?email= for one).
AGENT_DRAFT_CAP=

# ── Pipeline → Google Sheet (setter call-list) ───────────────────────────────
# Sincronizza la pipeline inbound (chi ha risposto + telefono + testo risposta)
# su un Google Sheet, in modo dinamico (cron /api/cron/pipeline + POST on-demand).
# Serve un SERVICE ACCOUNT Google con Sheets API abilitata; CONDIVIDI il foglio
# (Editor) con la sua email. Senza queste var il modulo non scrive (degrada).
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=
# Id del foglio (default = foglio pipeline Geriko). Opzionale: nome del tab.
PIPELINE_SHEET_ID=
PIPELINE_SHEET_TAB=

# Optional: comma-separated client definitions for quick setup WITHOUT Supabase.
# Format: slug|Display Name|instantly_api_key  (one client per entry)
# Example: acme|ACME Corp|inst_xxx,globex|Globex|inst_yyy
DASHBOARD_CLIENTS=

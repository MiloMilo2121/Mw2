/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The dashboard lives in a subfolder of a scratch repo; nothing exotic needed.
};

export default nextConfig;
